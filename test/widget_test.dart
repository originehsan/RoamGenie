// Basic smoke test — ensures the app entry point compiles and can be instantiated.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(true, isTrue);
  });
}
